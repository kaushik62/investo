{
  "name": "Investo",
  "version": "1.0.0",
  "description": "Investo - Virtual Indian Stock Market Simulator",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
